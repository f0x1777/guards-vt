# Rootstock Surface

This directory is the Rootstock-specific collaboration surface for Guards.

Subdirectories:
- `contracts/`: Solidity contracts and deployment tooling for Rootstock / RSK
- `offchain/`: Rootstock transaction builders, wallet flows, and protocol adapters

The first contract target is `GuardedTreasuryVault`.
